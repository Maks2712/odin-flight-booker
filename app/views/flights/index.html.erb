<h1>Flights#index</h1>
<p>Find me in app/views/flights/index.html.erb</p>
<span>
<div>
<select class="form-select" aria-label="Default select example">
  <option selected>Open this select menu</option>
  <option value="1">One</option>
  <option value="2">Two</option>
  <option value="3">Three</option>
</select>
</div>
<div>
<select class="form-select" aria-label="Default select example">
  <option selected>Open this select menu</option>
  <option value="1">One</option>
  <option value="2">Two</option>
  <option value="3">Three</option>
</select>
</div>
</span>

<%= form_with url: "/search", method: :get, data: { turbo: false} do |form| %>
  <%= form.label :flight_duration, "Search for:" %>
  <%= form.text_field :flight_duration %>
  <%= form.submit "Search" %>
<% end %>
<% @flight_results.each do |flight| %>
    <li>
      <%= link_to "#{event.title} #{event.created_at}", user_event_path(@user, event)%>
    </li>
  <% end %>





